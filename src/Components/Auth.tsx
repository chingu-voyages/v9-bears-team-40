import * as React from "react";

type IAccountProps = {
  toggleModal?: boolean;
};

const LogIn = (props: IAccountProps) => {
  return <button>click!</button>;
};

const LogOut = (props: IAccountProps) => {
  return <button>click!</button>;
};

export { LogIn, LogOut };
